[project]
name = "pyhanabi"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12, <3.13"
dependencies = [
    "numpy>=2.2.3",
]

[dependency-groups]
dev = [
    "mypy>=1.15.0",
    "ruff>=0.11",
]
