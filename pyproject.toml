[project]
name = "pyhanabi"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12, <3.13"
dependencies = [
    "dit>=1.5",
    "numpy~=1.0",
]

[dependency-groups]
dev = [
    "mypy>=1.16.0",
    "pre-commit>=4.2.0",
    "ruff~=0.12.1",
]
